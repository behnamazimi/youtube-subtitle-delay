'use strict';

const globalActions = {
  INIT: "INIT",
  POPUP_INIT: "POPUP_INIT",
  SET_OPTIONS: "SET_OPTIONS",
  OPTIONS_UPDATE: "OPTIONS_UPDATE",
}

let defaultDelay = 0

let defaultSelector = "#ytp-caption-window-container"
// let defaultSelector = "#ytp-caption-window-container>div:first-child"
